import React from 'react';
import Parent from './Parent';

const PassingProperties = () => {
    return (
        <div>
            <Parent/>
        </div>
    );
};

export default PassingProperties;